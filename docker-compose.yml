services:
  mariadb:
    image: mariadb:11.4
    container_name: movieDatabaseDB
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: movieDatabaseDB
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql
    networks:
      - movieDatabaseNetwork

volumes:
  mariadb_data:

networks:
  movieDatabaseNetwork:
    driver: bridge