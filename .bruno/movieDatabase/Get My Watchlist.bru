meta {
  name: Get My Watchlist
  type: http
  seq: 15
}

get {
  url: http://localhost:8081/api/watchlist
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
