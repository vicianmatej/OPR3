meta {
  name: Add To Watchlist
  type: http
  seq: 14
}

post {
  url: http://localhost:8081/api/watchlist/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: eq 200
  res.body.movieId: eq 1
  res.body.watched: eq false
}
